# 과제(12) 연결 리스트 삭제(deleteAt) 오류 수정 보고서

**학과:** 컴퓨터공학과 23학번  
**이름:** 김주혁  

## 1. 원본 코드의 오류 분석
제공된 `LinkedList.c`의 `deleteAtLinkedList` 함수에서 다음과 같은 논리적 오류를 발견했습니다.
1. **타겟 노드 탐색 오류:** 특정 인덱스(`at`)의 노드를 지우려면 직전 노드(`at - 1`)에서 `next` 포인터를 건너뛰도록 연결해야 합니다. 하지만 원본 루프는 `i < at`까지 돌아버려서 `nPtr`이 지울 노드 위치까지 넘어가 버리는 치명적인 버그가 있었습니다.
2. **리스트 크기 관리 누락:** 노드를 `free`로 메모리 해제한 뒤 전체 개수(`li->size`)를 감소시키는 로직이 아예 빠져 있었습니다.
3. **반환값 누락:** 함수 반환형이 `LinkedList*`인데 `return` 문이 없었습니다.

## 2. 수정 사항
- 유효성 검사(`if`)를 통과하여 삭제가 확정된 직후, 코드 상단에서 `li->size--;`를 먼저 수행하여 크기를 즉시 동기화했습니다.
- `for`문의 조건을 `i < at - 1`로 수정하여 포인터가 삭제 대상 노드의 바로 앞 노드에서 정확히 멈추도록 개선했습니다.
- 함수 끝에 `return li;`를 추가하여 안전하게 리스트 주소를 반환하도록 보완했습니다.
