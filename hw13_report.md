# 과제(13) 연결 리스트 메뉴 인터페이스 구현 보고서

**학과:** 컴퓨터공학과 23학번  
**이름:** 김주혁  

## 1. 과제 구현 개요
기존에 작성된 `LinkedList` 라이브러리를 활용하여 사용자가 쉽게 조작할 수 있는 `while`-`switch` 기반의 CLI 메뉴 프로그램을 작성했습니다. 입력받은 정수를 리스트에 안전하게 넣고 빼는 데 중점을 두었습니다.

## 2. 주요 로직 및 예외 처리
- **구조체 데이터 호환:** 공지사항의 조건은 정수를 추가하는 것이었으나, 기존 ADT의 노드(`PointType`) 구조는 2차원 좌표`(x, y)`를 요구합니다. 헤더를 갈아엎기보다는 실용적으로 접근하여, 입력받은 정수를 `item.x`에 저장하고 `item.y = 0`으로 고정하여 기존 함수(`insertFirstLinkedList` 등)들을 그대로 재활용했습니다.
- **특정 위치 삽입 (메뉴 3번):** 사용자가 원하는 위치(`pos`)에 노드를 넣기 위해 `for` 루프를 돌려 `pos-1` 번째 노드 포인터(`pre`)를 찾아낸 뒤, `insertItemLinkedList` 함수를 호출하도록 구현했습니다. 
- **삭제 값 출력 (메뉴 4번):** `deleteAtLinkedList` 함수는 리스트의 포인터만 갱신할 뿐 삭제된 데이터를 반환하지 않습니다. 따라서 삭제 함수를 호출하기 직전에 타겟 인덱스까지 `for` 루프로 순회하여 노드의 `x` 값을 백업해두고, 삭제가 끝난 후 출력하도록 로직을 구성했습니다.

## 3. 메모리 관리
0번을 눌러 종료할 때 반드시 `destroyLinkedList`를 호출하여 리스트 전체 노드와 헤드 포인터에 대한 `free()` 작업이 수행되도록 하여 메모리 누수를 방지했습니다.
