# 과제(15) Linked Stack을 활용한 웹 브라우저 히스토리 구현 보고서

**학과:** 컴퓨터공학과 23학번  
**이름:** 김주혁  

## 1. 구현 개요
제공된 ADT(create, destroy, push, pop, empty, full, print, peek)를 완벽하게 구현하여 동적 연결 스택(Linked Stack)을 구성했습니다. 이 스택을 활용하여 사용자가 URL을 추가하면 창이 열리고(Push), 제거하면 최근 창이 닫히며(Pop) 이전 창으로 돌아가는 웹 브라우저의 동작 방식을 모사했습니다.

## 2. 자료구조적 특징 및 함정 회피 (LIFO)
- 과제 명세 2번 항목에 "URL을 입력받아 제거한다"는 설명이 있었으나, 특정 데이터를 찾아 지우는 행위는 스택의 본질적인 원칙인 LIFO(후입선출)를 위배하는 것입니다.
- 따라서 명세서의 입출력 예시 로직이 증명하듯, `pop` 연산 시 별도의 입력을 받지 않고 스택의 `top`에 있는 가장 최근 URL을 곧바로 이탈(pop)시키도록 정석대로 구현했습니다.
- `isFull` 연산의 경우 연결 리스트 기반이므로 배열과 달리 포화 상태가 존재하지 않아 항상 0을 반환하도록 처리했습니다.

## 3. 메모리 관리 안전성
스택이 비워지는(`isEmpty() == 1`) 순간 "브라우저가 종료되었습니다" 메시지를 띄움과 동시에 반복문을 종료하며, 즉시 `destroyStack()` 함수를 호출하여 할당된 연결 구조체들의 메모리 누수를 완전히 차단했습니다.
