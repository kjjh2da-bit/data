# 과제(16) Calculator 코드 개선 및 완성 보고서

**학과:** 컴퓨터공학과 23학번  
**이름:** 김주혁  

## 1. 수업시간 코드 오류 개선 
기존 `infixToPostfix` 뼈대 코드에서 괄호 처리와 연산자 우선순위 비교에 대한 논리적 오류를 바로잡았습니다.
- `precedence` 함수를 통해 우선순위를 명확히 규정하고, 새로운 연산자가 스택에 들어갈 때 자신보다 우선순위가 높거나 같은 연산자들은 모두 `pop`하여 출력 큐(Postfix)로 보내도록 반복문(`while`)을 수정했습니다.
- 과제 명세에 맞추어 문자열을 순회할 때마다 현재까지 완성된 `Postfix` 문자열과 `Stack` 내부에 저장된 연산자 상태를 실시간으로 출력하도록 개선했습니다.

## 2. evalPostfix 동작 로직 구현 
누락되어 있던 후위 표기식 계산 함수를 구현했습니다.
- 피연산자(숫자 문자)를 만나면 `ch - '0'` 연산을 통해 실제 정수형(`int`) 데이터로 변환하여 스택에 `push` 합니다.
- 연산자를 만나면 스택에서 두 번 `pop`을 수행하여 연산(`op1`, `op2`)을 진행하고, 그 결과값을 다시 스택에 넣습니다. (이때 먼저 pop된 숫자가 연산 기호 뒤로 가야 함을 주의 깊게 코딩했습니다.)
- 최종적으로 스택에 단 1개의 값만 남게 되며, 이를 `pop`하여 리턴함으로써 식의 최종 결괏값을 도출합니다.
